export function WorkspaceHeader({ darkMode, onToggleTheme, mobileMenuOpen, onToggleMenu, notificationsOpen, onToggleNotifications, notifications }) {
  return <header className="workspace-header"><div><p className="kicker">JUEVES, 04 DE SEPTIEMBRE</p><h1>Radio Universitaria <span>↗</span></h1></div><div className="header-actions"><button type="button" title={darkMode ? 'Activar modo claro' : 'Activar modo oscuro'} aria-label={darkMode ? 'Activar modo claro' : 'Activar modo oscuro'} onClick={onToggleTheme}>{darkMode ? '☀' : '☾'}</button><div className="notification-wrap"><button className={notificationsOpen ? 'notification-active' : ''} type="button" title="Notificaciones" aria-label="Abrir notificaciones" aria-expanded={notificationsOpen} onClick={onToggleNotifications}>♧<i /></button>{notificationsOpen && <div className="notification-panel"><strong>Notificaciones</strong>{notifications.map((notification, index) => <p key={`${notification}-${index}`}>{notification}</p>)}</div>}</div><button type="button" className="mobile-menu" aria-label={mobileMenuOpen ? 'Cerrar menú' : 'Abrir menú'} onClick={onToggleMenu}>{mobileMenuOpen ? '×' : '☰'}</button></div></header>
}
