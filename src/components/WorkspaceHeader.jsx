export function WorkspaceHeader({language,labels,mobileMenuOpen,onLanguageChange,onToggleMenu}){
 return <header className={'workspace-header'}><div className={'mobile-brand'}><span>RH</span>{labels.stationName}</div><div className={'header-actions'}><div className={'language-switch'} aria-label={'Language'}><button className={language==='es'?'selected':''} type={'button'} onClick={()=>onLanguageChange('es')}>ES</button><button className={language==='hnh'?'selected':''} type={'button'} onClick={()=>onLanguageChange('hnh')}>HN</button></div><button className={'mobile-menu'} type={'button'} onClick={onToggleMenu} aria-expanded={mobileMenuOpen} aria-label={mobileMenuOpen?'Close menu':'Open menu'}>{mobileMenuOpen?'x':'Menu'}</button></div></header>
}
